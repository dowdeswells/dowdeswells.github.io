version: '3.8'

services:
  jekyll:
    image: jekyll/jekyll:pages
    command: jekyll serve --livereload
    ports:
      - "4000:4000"
      - "35729:35729" # Enables LiveReload web sockets
    volumes:
      - .:/srv/jekyll:Z